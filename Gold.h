/*#ifndef GOLD_H
#define GOLD_H

#include "TextureManager.h"

    class Gold {
    private:
        TextureManager textureManager;

    public:
        int maxGold;
        int currentGold;

        Gold(sf::Vector2f GoldPosition, int maxGold);// Forward declaration of the constructor
    };
#endif


#include <iostream>

// Declaration of the inline function
inline int add(int currentgeld, int inkomendgeld) {
    return currentgeld + inkomendgeld;
}

int main() {
    // Using the inline function
    int Gold = add(5, 7);
    window.draw(nummergeld)
        // Output the result
        std::cout << "Result of adding 5 and 7: " << result << std::endl;

    return 0;
    */

